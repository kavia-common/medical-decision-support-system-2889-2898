"""
Views package initializer for api.views_chat.

Intentionally left without re-exports to avoid circular or partial imports.
Import view functions directly from api.views_chat (module file) in urlconfs:
    from .views_chat import chat, recommend, upload_report, get_recommendation
"""
