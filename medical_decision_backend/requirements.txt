# Pin core Django and DRF stack to compatible, widely available versions.
# Notes:
# - Removed invalid custom environment markers (e.g., os_environment) which pip cannot parse.
# - Optional heavy deps (faiss-cpu, chromadb) are left unpinned here; the code handles their absence gracefully.
#   If needed, install them explicitly in environments that support their system dependencies.

asgiref==3.8.1
Django==5.0.6
djangorestframework==3.15.2
drf-yasg==1.21.7
django-cors-headers==4.4.0
sqlparse==0.5.1
PyYAML==6.0.2
uritemplate==4.1.1

# Optional vector DB libraries (install manually if required by your environment):
# faiss-cpu==1.8.0.post1
# chromadb==0.5.5

# Dev/QA tooling (optional)
flake8==7.1.1
mccabe==0.7.0
pycodestyle==2.12.1
pyflakes==3.2.0
packaging==24.1
pluggy==1.5.0
iniconfig==2.0.0
typing-extensions==4.12.2

# WSGI server (optional but commonly used)
gunicorn==22.0.0
